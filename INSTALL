You will have to modify the Makefile to point to the AMOS include and library directory. 
After that, just type "make" and it will compile. Afterwards, you may move
extractScaffold to whereever you want.

